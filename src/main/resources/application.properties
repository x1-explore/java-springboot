# 数据库配置
spring.datasource.url=jdbc:mysql://bj-cdb-5gymlg84.sql.tencentcdb.com:28139/admin_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=admin_user
spring.datasource.password=admin123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# 服务器配置
server.port=8080

# Thymeleaf配置
spring.thymeleaf.cache=false

# 允许循环依赖
spring.main.allow-circular-references=true

# 文件上传配置
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# 静态资源访问配置
spring.web.resources.static-locations=classpath:/static/,classpath:/public/,file:uploads/
spring.mvc.static-path-pattern=/**

# 文件上传目录配置
app.upload.path=uploads
app.upload.avatar-dir=avatars 